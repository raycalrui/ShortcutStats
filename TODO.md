# ShortcutStats 待办事项

目标：逐步成为原生、免费、本地存储的 Mac 使用统计工具，覆盖接近 WhatPulse 的核心键鼠与应用统计体验，并提供适合内容创作的展示与导出功能。

本文是开发路线，不代表功能已实现，也不代表授权立即执行全部任务。开始每项工作前检查当前代码与已有修改，完成实现和对应验证后再勾选。README 的功能与限制说明保持中英一致。

## 优先级与执行顺序

| 优先级 | 含义 |
| --- | --- |
| P0 | 最高：保证长期统计可靠，或是后续功能的必要前提 |
| P1 | 高：核心统计能力，优先安排 |
| P2 | 中：完善体验、展示与分发 |
| P3 | 后续评估：成本或范围较大，单独确认后安排 |

阶段决定总体顺序，优先级决定阶段内的先后；有依赖时先完成依赖。

建议下一版范围：设置页 → 登录启动 → 监听恢复 → 应用排除 → 自定义日期 → 每日趋势。每日趋势属于第二阶段，可提前使用现有按天数据实现。

## 第一阶段：可靠地长期记录快捷键

阶段目标：让用户可以持续记录数周，知道何时在统计、何时存在缺失，并能管理统计范围和保护已有数据。

- [ ] **S1-01 · P0 · 设置页面**：集中管理登录启动、统计范围、暂停状态及数据位置。验收：设置重启后保留，显示状态与实际行为一致。
- [ ] **S1-02 · P0 · 登录时自动启动**：提供用户可控制的登录启动开关。验收：启用后登录自动运行，关闭后不启动；启动时尊重暂停偏好和权限状态。
- [ ] **S1-03 · P0 · 监听状态与恢复**：处理睡眠唤醒、权限变化、事件监听中断和安全输入状态。验收：能恢复的自动恢复，无法恢复的明确提示；不绕过权限或安全输入，不把缺失时段当作完整统计。
- [ ] **S1-04 · P1 · 应用排除名单**：允许选择不采集的应用，例如游戏或远程桌面。验收：按稳定的应用标识匹配，仅影响后续采集，已有记录保留。
- [ ] **S1-05 · P1 · 自定义日期范围**：选择起止日期，统一排行榜与导出范围。验收：按 Mac 本地日历处理边界，跨月、单日和空结果正确，导出与界面一致。
- [ ] **S1-06 · P1 · 排行榜搜索与隐藏项**：搜索组合键，并可隐藏复制、粘贴等指定项目。验收：隐藏仅影响展示及明确标注的展示导出，不删除原始计数；支持恢复显示，与采集排除分开。
- [ ] **S1-07 · P1 · 备份与恢复**：支持本地导出备份和恢复。验收：备份可验证完整性，恢复前保护当前数据；明确合并或替换规则，损坏备份不覆盖有效数据。
- [ ] **S1-08 · P0 · 持续运行验收**：以合成样本验证计数与保存，并进行真实日常使用验证。记录重启、唤醒、暂停及权限中断结果，区分已测行为和未测边界；测量后台 CPU、内存和写盘情况，不用主观感受替代数据。

阶段完成标准：记录与暂停行为可靠，中断可见，历史可恢复，筛选与导出一致；已知限制写入 README。

## 第二阶段：补齐键鼠与应用使用统计

阶段目标：提供键盘热力图、历史趋势、鼠标统计、应用活跃时长及统一总览，接近 WhatPulse 的核心本地统计用途。

- [ ] **S2-01 · P0 · 明确新增数据口径和开关**：为普通按键计数、鼠标及应用时长分别定义采集范围、开关、重复计数规则和保留粒度。默认仍只采集快捷键，普通按键统计明确选择后才启用；不记录文字或原始按键顺序。
- [ ] **S2-02 · P0 · SQLite 存储与旧数据迁移**：在引入小时趋势、鼠标和应用时长前设计本地数据库与批量写入。保留原 JSON 备份，自动导入已有每日快捷键记录，防止重复导入，验证总数与排名一致，失败时可恢复；不伪造旧记录的小时分布。实施时同步更新 AGENTS.md 和 README 中的存储说明。
- [ ] **S2-03 · P1 · 每日趋势**：展示按天用量与周期变化，支持应用筛选。验收：图表汇总与排行榜一致；区分确实没有使用和未采集，已有每日数据可直接使用。可提前纳入下一版。
- [ ] **S2-04 · P1 · 每小时趋势**：展示一天中各小时的活动分布。依赖新增时间粒度和存储方案。验收：说明记录开始时间，正确处理本地时间边界，旧的按天数据不推算成小时记录。
- [ ] **S2-05 · P1 · 键盘热力图**：展示各物理键使用频率，支持全局和单应用查看。依赖 S2-01；仅累加次数，不保存内容或顺序。验收：热力图与键次数列表一致，开关关闭后停止新增采集，不把普通按键当作快捷键。
- [ ] **S2-06 · P1 · 鼠标统计**：统计左右键次数、滚动量和移动距离。先明确单位、滚动方向与设备差异；移动距离以像素等可核实单位展示，不宣称精确物理距离。验收：移动事件合并、批量保存，暂停和应用排除生效，评估高频输入下的资源占用。
- [ ] **S2-07 · P1 · 应用使用时长**：统计前台应用活跃时间，区分空闲、锁屏和睡眠。验收：空闲阈值明确且可说明，前台切换正确切分时段，不把进程存活时间等同于使用时间。
- [ ] **S2-08 · P1 · 统计总览**：统一展示按键、快捷键、点击、活跃时长和常用应用。依赖对应指标实现。验收：共享时间范围与口径，未开启的指标显示未采集，而非误导性的零。
- [ ] **S2-09 · P2 · 多键盘布局**：支持布局选择或检测，明确区分物理键位和输入字符。验收：切换布局和输入法后显示正确，历史数据有明确解释，不混淆不同口径的记录。
- [ ] **S2-10 · P1 · 数据增长与性能验证**：使用较长时间跨度的合成数据检查查询、导出、备份和迁移；验证批量写入和后台采集不会随界面刷新频率增加负担，公布实际测量条件与限制。

阶段完成标准：所有新增采集均有明确口径与开关，历史兼容，统计页面之间可核对，长时间运行和数据增长已有验证。

## 第三阶段：内容创作特色与易用分发

阶段目标：让统计结果容易理解、对比和分享，让开源用户更方便安装使用。

- [ ] **S3-01 · P1 · 排行榜图片导出**：导出适合视频、B 站动态及社交平台的 Top 10 图片。验收：带统计时间范围、次数和必要的筛选说明，长名称不截断，清晰度适合分享；不默认暴露应用列表等个人信息。
- [ ] **S3-02 · P1 · 快捷键自定义名称**：为组合键设置名称，例如“打开 Raycast”，支持按应用分别命名。验收：名称只用于展示，不改变原始组合键和计数；应用专属名称与全局名称的优先级明确。
- [ ] **S3-03 · P1 · 周期对比**：比较本周与上周或两个自选周期，展示增减和排名变化。验收：零基数、不同周期长度与缺失数据处理清楚，不生成误导性的增长百分比。
- [ ] **S3-04 · P2 · 中英文界面**：跟随系统语言，覆盖主界面、设置、权限说明、错误信息与导出标签。验收：两种语言信息一致，布局无溢出，数字和日期格式正确。
- [ ] **S3-05 · P2 · 签名、公证和安装包**：准备适合 GitHub 用户下载的 App / DMG 及安装说明。先确认开发者账号、证书及分发条件；保留本地自用构建方式。验收：在目标环境验证安装、首次启动、权限引导和升级数据兼容；签名材料不入库，实际公开发布另行授权。
- [ ] **S3-06 · P3 · 自动识别快捷键名称的可行性调研**：评估应用菜单、自定义快捷键和后台工具的识别范围。先输出准确性边界及权限需求，再决定是否实现；无法识别时允许手动命名，不宣称覆盖全部命令。

阶段完成标准：可导出准确且清晰的分享素材，双语界面完整，安装和升级流程有实际验证，公开文档与功能状态一致。

## 三阶段之后：独立评估的扩展

以下功能尚未纳入三个阶段的实施范围。它们涉及新的权限、采集范围或持续维护成本，需分别确认方案。

- [ ] **P3 · 网络流量统计**：评估总流量、按应用流量及其归属精度、权限和后台成本。
- [ ] **P3 · 网站使用统计**：评估浏览器支持、域名级时长和隐私模式排除；先明确必要的数据范围，不默认采集网页内容或完整地址。
- [ ] **P3 · 多设备云同步**：评估是否确有需要，再选择存储、加密、冲突合并和删除策略；保持纯本地使用可用。
- [ ] **P3 · 公开排行榜与账号系统**：单独评估服务器成本、用户同意、数据公开范围及维护责任，不默认上传统计。

这些扩展如果获批，须同步调整项目当前“无网络请求、无账号、纯本地”的约束与隐私说明。

## 完成一项任务时

- [ ] 核对实现与本项验收标准，记录实际验证及未验证部分。
- [ ] 涉及统计、存储或权限的修改补充针对性检查，使用合成样本或隔离副本保护真实数据。
- [ ] 更新必要的中英 README 和 agent 指引；不得把计划功能写成已实现。
- [ ] 仅在完成验证后勾选对应任务；提交与推送按当次用户授权处理。
